#pragma once

// Единственное место, где меняется номер версии при релизе.
// Формат -- на ваше усмотрение (semver, дата и т.п.), сейчас просто X.Y.Z.
#define FIRMWARE_VERSION "1.18.1"

// Дата и время сборки -- подставляются компилятором автоматически,
// удобно, чтобы отличить два билда с одинаковым FIRMWARE_VERSION
// (например, если забыли его поднять перед пересборкой).
#define FIRMWARE_BUILD_DATE __DATE__ " " __TIME__
